PORT=4000
NODE_ENV=development

# ---- 鉴权 ----
# 生产环境务必改成 64 位随机字符串：openssl rand -hex 32
JWT_SECRET=dev-insecure-please-override-in-production
JWT_EXPIRES_IN=7d

# ---- 微软 OAuth2 ----
# Token 端点（通常无需修改）
MS_TOKEN_ENDPOINT=https://login.microsoftonline.com/common/oauth2/v2.0/token
